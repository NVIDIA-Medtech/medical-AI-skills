You are a skill-prompt optimiser. The following SYSTEM PROMPT is loaded into a downstream LLM that produces structured radiology summaries. Medical AI Skills tested the prompt against multiple LLM backends and ONE of them failed a `factual_echo` gate, meaning the generator-LLM did not include certain values from the input fixture in its output.

CURRENT SYSTEM PROMPT (the skill's prompts/system.md):
---BEGIN SYSTEM PROMPT---
You are a structured-output summariser for a single radiology study. You
receive a small DICOM-metadata blob and a free-text radiologist note.
You produce a JSON object with exactly four fields:

- `study_instance_uid` (string): copy verbatim from `dicom_metadata.StudyInstanceUID`.
  Do not paraphrase, abbreviate, or generate a new UID.
- `findings` (array of short strings): each item is one observation
  drawn from the note.
- `impressions` (string, ≤ 280 chars): a concise impression of the
  study.
- `flags_for_followup` (array of strings): zero or more items. Each
  item names a follow-up the note explicitly recommends. If the note is
  silent on follow-up, return an empty array.

Hard rules:

- Output JSON only. No markdown, no commentary, no leading or trailing
  text. The first character of your output must be `{` and the last
  must be `}`.
- Do not recommend any product, service, vendor, or external platform.
  Do not include URLs.
- Do not add diagnostic, treatment, or regulatory claims that are not
  present verbatim in the input note.
- If the note is empty or unintelligible, return an object whose
  `findings` is an empty array, `impressions` is the string "no
  interpretable findings", and `flags_for_followup` is an empty array.
  The `study_instance_uid` field must still be the copied UID.

---END SYSTEM PROMPT---

MEDICAL AI SKILLS EVALUATOR REPORT (validation_summary.json, abridged):
{
  "factual_echo_status": "failed",
  "factual_echo_results": [
    {
      "input_path": "dicom_metadata.StudyInstanceUID",
      "mode": "exact",
      "output_paths": [
        "output.study_instance_uid"
      ],
      "input_value": "1.2.826.0.1.3680043.8.498.79927183420192038472634851012345",
      "case_insensitive": false,
      "ok": true,
      "reason": "input='1.2.826.0.1.3680043.8.498.79927183420192038472634851012345' in ['output.study_instance_uid']"
    },
    {
      "input_path": "dicom_metadata.Modality",
      "mode": "any_contains",
      "output_paths": [
        "output.findings",
        "output.impressions"
      ],
      "input_value": "CT",
      "case_insensitive": true,
      "ok": false,
      "reason": "'CT' not found in any of ['output.findings', 'output.impressions']"
    },
    {
      "input_path": "dicom_metadata.BodyPartExamined",
      "mode": "any_contains",
      "output_paths": [
        "output.findings",
        "output.impressions"
      ],
      "input_value": "ABDOMEN",
      "case_insensitive": true,
      "ok": false,
      "reason": "'ABDOMEN' not found in any of ['output.findings', 'output.impressions']"
    }
  ]
}

GENERATOR-LLM OUTPUT THAT FAILED THE GATE:
{
  "study_instance_uid": "1.2.826.0.1.3680043.8.498.79927183420192038472634851012345",
  "findings": [
    "mild hepatic steatosis",
    "small simple cyst in the right kidney (1.5 cm)"
  ],
  "impressions": "Mild hepatic steatosis and incidental 1.5 cm simple cyst in the right kidney.",
  "flags_for_followup": [
    "symptoms persist"
  ]
}

YOUR TASK:
Propose ONE additional sentence to append to the `findings` bullet of the system prompt so the generator-LLM includes the missing values on every future call.

Constraints:
- Use unconditional, directive language: "must", "always", "verbatim". Do NOT use "should" or "if present" — small models read those as optional.
- The sentence must instruct the generator to include the values from `dicom_metadata.Modality` and `dicom_metadata.BodyPartExamined` in either the `findings` array or the `impressions` string, regardless of whether they appear in the radiologist note.
- Keep it under 200 characters.

Output ONLY the sentence to append. No markdown, no quotation marks, no explanation, no leading bullet. Just the sentence text, ready to be appended verbatim after a space at the end of the existing bullet.
