Synthetic fixture note.

The verifier's executable fixtures are generated in `tests/test_grade.py` with
small NumPy/NiBabel arrays. They are kept in test temp directories instead of
committing NIfTI volumes here. No patient data belongs in this directory.
