# Fully on-box NeMo Anonymizer providers (PHI-safe: no text leaves host).
# GLiNER on Windows GPU; LLM roles on local Ollama.
providers:
  - name: local-gliner
    endpoint: http://172.20.0.1:8001/v1
    provider_type: openai
    api_key: EMPTY

  - name: local-ollama
    endpoint: http://172.20.0.1:11434/v1
    provider_type: openai
    api_key: EMPTY
