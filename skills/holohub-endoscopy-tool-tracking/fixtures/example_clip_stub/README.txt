Stub directory for testing the wrapper's setup-error paths.

Real GXF Stream Replayer fixtures (.gxf_index + .gxf_entities pairs)
are not committed; see ../README.md for how to use the upstream
sample (`--fixture default`) or how to point the wrapper at your own
GXF replayer pair.

This file exists so the directory is committed under git.
